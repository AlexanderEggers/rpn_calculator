package org.demo.calculator

/**
 * Main function which is executed when the project has been started. The main function will start the loop from the
 * [CalculatorApp].
 */
fun main(args: Array<String>) {
    CalculatorApp().run()
}